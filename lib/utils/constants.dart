import 'package:flutter/material.dart';

class UiConstants {
  static const double screenHorizontalPadding = 12;
  static const double postVerticalGap = 12;
  static const double avatarRadius = 16;
  static const double storyAvatarRadius = 30;
  static const double iconSize = 24;
  static const Duration fastAnimation = Duration(milliseconds: 180);
  static const Duration mediumAnimation = Duration(milliseconds: 280);
}

class AppColors {
  static const Color pageBackground = Colors.white;
  static const Color divider = Color(0xFFE8E8E8);
  static const Color primaryText = Color(0xFF0F0F0F);
  static const Color secondaryText = Color(0xFF6E6E6E);
}
